# Installed as /etc/needrestart/conf.d/am2-realtime.conf.
# Realtime WebSocket services require a controlled session-drain window.
$nrconf{override_rc}->{qr(^am2-api\.service$)} = 0;
$nrconf{override_rc}->{qr(^am2-api-staging\.service$)} = 0;

1;
